#include <stdio.h>

void binario (int);

int main (){
  int n;
  printf ("digite o numero a ser convertido: ");
  scanf ("%d", &n);
  printf ("binario: ");
  binario (n);
}

void binario (int n){
  if (n/2==0){
    printf ("%d", n%2); 
  }
  else {
    binario (n/2);
    printf ("%d", n%2);
  }
}
